/**
 * EXEMPLO DE UTILIZAÇÃO DA 'comprarCarta'
 * 
 * 
    const carta = comprarCarta(); // Sorteia uma carta. Por exemplo, o rei de ouros
    
    console.log(carta.texto) // imprime o texto da carta. Exemplo: "K♦️" (indica "K" de ouros)
    console.log(carta.valor) // imprime o valor da carta (um número). Exemplo: 10 (dado que "K" vale 10)
 * 
 * 
 * 
 */

   //  let primeiraCartaUsuario  
   //  let segundaCartaUsuario 
   //  let primeiraCartaComputador 
   //  let segundaCartaComputador 

    
   //  if(confirm("BEM VINDO AO JOGO DE BLACKJACK!\nQuer iniciar uma nova rodada?")) {
   //    primeiraCartaUsuario = comprarCarta()
   //    segundaCartaUsuario = comprarCarta()
   //    primeiraCartaComputador = comprarCarta()
   //    segundaCartaComputador = comprarCarta()
     
   //    while ((primeiraCartaUsuario.valor === 11 === segundaCartaUsuario.valor) || (primeiraCartaComputador.valor === 11 === segundaCartaComputador.valor)) {   
   //       primeiraCartaUsuario = comprarCarta()
   //       segundaCartaUsuario = comprarCarta()
   //       primeiraCartaComputador = comprarCarta()
   //       segundaCartaComputador = comprarCarta()
       
   //    }  
   //    console.log(primeiraCartaUsuario)
   //    let maoUsuario = []
   //    let maoComputador = []

   //       maoUsuario.push(primeiraCartaUsuario)
   //       maoUsuario.push(segundaCartaUsuario)
   //       maoComputador.push(primeiraCartaComputador)
   //       maoComputador.push(segundaCartaComputador)

   //       console.log (maoUsuario)
   //       console.log (maoComputador)

   //     let pontosDoUsuario = primeiraCartaUsuario.valor + segundaCartaUsuario.valor
   //     let pontosDoComputador = primeiraCartaComputador.valor + segundaCartaComputador.valor
              
          
   //        while ((pontosDoUsuario < 21) && confirm(
   //        console.log(`Suas cartas são: ${maoUsuario.map((item) => {return item.texto})} - ${pontosDoUsuario} pontos. A carta revelada do computador é ${primeiraCartaComputador.texto} - ${primeiraCartaComputador.valor} pontos. \nDeseja comprar mais uma carta?`))) { 
   //        let novaCartaUsuario = comprarCarta()
   //        maoUsuario.push(novaCartaUsuario)
   //        pontosDoUsuario += novaCartaUsuario.valor
   //       }
   //        console.log (pontosDoUsuario) 
   //        console.log (pontosDoComputador)  

   //          if (21 >= pontosDoUsuario && pontosDoUsuario > pontosDoComputador || pontosDoComputador > 21) {
                  
   //                console.log(`Suas cartas são: ${maoUsuario.map((item) => {return item.texto})}. Sua pontuação é: ${pontosDoUsuario}\nAs cartas do computador são ${maoComputador.map((item) => {return item.texto})}. A pontuação do computador é: ${pontosDoComputador}. O usuário ganhou!`)

   //          } else if (21 >= pontosDoComputador && pontosDoComputador > pontosDoUsuario || pontosDoUsuario > 21) {
                  
   //                console.log(`Suas cartas são: ${maoUsuario.map((item) => {return item.texto})}. Sua pontuação é: ${pontosDoUsuario}\nAs cartas do computador são ${maoComputador.map((item) => {return item.texto})}. A pontuação do computador é: ${pontosDoComputador}.O computador ganhou!`)

   //          } else if (pontosDoUsuario === pontosDoComputador) {
                  
   //                console.log(`Suas cartas são: ${maoUsuario.map((item) => {return item.texto})}. Sua pontuação é: ${pontosDoUsuario}\nAs cartas do computador são ${maoComputador.map((item) => {return item.texto})}. A pontuação do computador é: ${pontosDoComputador}.Empate!`)
   //          }} else {
   //             console.log ("O jogo acabou!")
            
   //           }

         